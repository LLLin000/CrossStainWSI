[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crossstainwsi"
version = "0.2.0"
description = "Auditable cross-stain whole-slide image registration toolkit"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
